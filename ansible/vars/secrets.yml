---
# Sensitive Variables - ENCRYPT THIS FILE WITH ansible-vault
# Usage: ansible-vault create vars/secrets.yml
# Usage: ansible-vault edit vars/secrets.yml

# Docker Registry Credentials
docker_username: deekshith1809
docker_password: "YOUR_DOCKER_PASSWORD"  # Replace with actual password

# MySQL Credentials
mysql_root_password: "Deekshith@123"
mysql_user: "root"
mysql_password: "Deekshith@123"

# Application Secrets
app_jwt_secret: "your-jwt-secret-key-here"
app_api_key: "your-api-key-here"

# SSL/TLS Certificates (if using HTTPS)
ssl_cert_path: "/etc/ssl/certs/artgallery.crt"
ssl_key_path: "/etc/ssl/private/artgallery.key"
